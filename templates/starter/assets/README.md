# Application assets

Place user-owned admitted images, fonts and audio here, then declare each resource in `luastra.json`. Packaging is implemented; typed Luau asset references and media components are available through the public SDK.
